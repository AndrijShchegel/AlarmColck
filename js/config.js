'use strict';

const alarmSettings = {
    timeUnits: ["hours", "minutes", "seconds"],
    timeNumber: [24, 60, 60],
    div: ["select", "button"],
    button: ["set", "clear", "del"],
    interval: [],
    index: 0
  };
  